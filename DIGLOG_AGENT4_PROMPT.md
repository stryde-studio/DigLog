# DigLog — Replit Agent 4用プロンプト（廃止）

> このプロンプトはReact Native / Expo案の検討用に作成したものです。現在のDigLogはSwiftUI / Xcodeで開発する方針のため、当日の実装には使用しません。

以下の内容をReplit Agent 4に貼り付けて、DigLogのMVPを実装する。

---

## プロンプト

あなたはDigLogのMVP実装担当です。

このプロジェクトは、旅行の開始日と終了日を選ぶだけで、端末内の写真からTravel Journalを自動生成するiOS向けモバイルアプリです。

今回の目的は、1日イベントで発表できる、動作する美しいMVPを完成させることです。

### 技術方針

- React Native / ExpoでiOS向けモバイルアプリを作る
- TypeScriptを使う
- 外部サーバーを使わない
- 外部AIを使わない
- RevenueCatや課金機能は実装しない
- 印刷サービスとの連携は実装しない
- まずはアプリ内に用意したサンプル写真で確実に動く状態にする
- 必要に応じて、後からiPhoneの写真ライブラリ連携を追加できる構造にする

### プロダクトの核

ユーザーに写真を1枚ずつ選ばせない。

ユーザーが行う操作は、旅行の開始日と終了日を選ぶことだけにする。

その後、アプリが自動的にTravel Journalを生成する。

### 必須画面

1. ホーム画面
2. 旅行の開始日・終了日選択画面
3. アルバム生成中画面
4. Travel Journal閲覧画面
5. 印刷用PDFへ進むための画面

### 必須機能

- 日付選択
- サンプル写真の自動読み込み
- 選択期間に応じた写真の自動抽出
- 写真量に応じたページ数の自動生成
- 表紙の生成
- Dayページの生成
- 最終ページの生成
- 電子書籍のようなページめくりUI
- 左右スワイプでページ移動
- タップでも次ページへ移動
- 写真・地名・日付を使ったシンプルなタイトル表示
- 印刷用PDFの生成、またはPDF出力導線

### デザイン方針

Travel Journalは、Pinterestのようなスクラップブック風にする。

- 紙のような背景
- 大きい写真と小さい写真の組み合わせ
- コラージュ
- 余白
- テープや紙の質感
- 表紙から始まる本らしい構成
- ページをめくる楽しさが伝わるアニメーション

単なる写真一覧やグリッド表示にはしない。

ユーザーが「本を読んでいる」と感じられるUIにする。

### ページ生成ルール

- ページ数は固定しない
- 写真量と旅行日数に応じてページ数を増減させる
- 写真が少ない日は余白を活かす
- 写真が多い日は複数ページに分ける
- デモでは2〜3日分のサンプル旅行を使う
- サンプル写真は、旅行の日付に対応するデータとして扱う

### 重要なFallback

写真ライブラリ連携や自動処理で問題が起きてもデモできるようにする。

- アプリ内にサンプル写真を同梱する
- 固定のサンプル旅行データを用意する
- 生成済みのページを表示するFallbackを用意する
- 写真が0枚でもクラッシュせず、適切なメッセージを表示する

### 作らない機能

- SNS共有
- アカウント
- クラウド同期
- AI旅行プラン
- 動画生成
- RevenueCat購入画面
- 印刷サービス連携
- 高度なAIタイトル生成

### 実装の進め方

1. 既存プロジェクトの状態を確認する
2. Expoアプリが起動する状態を作る
3. 画面遷移を作る
4. サンプル写真で日付選択からアルバム生成までつなぐ
5. ページめくりUIを実装する
6. レイアウトとアニメーションを磨く
7. PDF出力またはPDF導線を追加する
8. iOSシミュレーターまたはExpo Goで操作確認する
9. 起動エラー、画面遷移ミス、ボタンの反応、型エラー、Expoビルドエラーを修正する
10. `git diff`を確認し、変更内容を整理する
11. GitHubへcommit・pushする

### 完了条件

以下をすべて満たしたらMVP完成とする。

- アプリが起動する
- ホームから日付選択画面へ進める
- 開始日と終了日を選べる
- 生成ボタンが動く
- アルバム生成画面が表示される
- 表紙が表示される
- Dayページが表示される
- 左右スワイプでページをめくれる
- タップでもページを進められる
- 写真が見切れずに表示される
- ページ数が写真量に応じて変わる
- 空データでもクラッシュしない
- PDF出力またはPDF出力への導線がある
- 明らかな起動・型・ビルドエラーがない

### 最後に

完璧な製品を作ろうとせず、1日で発表できるMVPを完成させてください。

最優先は、次の体験です。

> 旅行の日付を選ぶ
> ↓
> 写真を選ばずにアルバムが生成される
> ↓
> 本のようにページをめくる

この体験が安定して動くことを確認したら、GitHubへcommit・pushしてください。

---

## Agent 4完了後の引き継ぎ

1. Agent 4がGitHubへpushする
2. ローカル環境へcloneまたはpullする
3. Claude Code DesktopがiOSシミュレーターで実操作レビューする
4. Codexが難所の修正と最終品質保証を担当する
