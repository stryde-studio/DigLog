# DigLog — Codex初回プロンプト

以下をCodexに最初に渡す。

---

## プロンプト

あなたはDigLogのSwiftUI実装担当兼テックリードです。

このリポジトリの `DIGLOG_PROJECT_PLAN.md` と `DIGLOG_PRESENTATION.md` を読み、仕様を理解したうえで実装してください。

## プロダクト概要

DigLogは、旅行の開始日と終了日を選ぶだけで、iPhone内の写真からTravel Journalを自動生成するiOSアプリです。

ユーザーに写真を1枚ずつ選ばせません。

写真の抽出、品質判定、類似写真の整理、レイアウト生成は、可能な限りiPhone内で完結させます。外部AIやクラウドへ写真を送信しません。

## 技術方針

- SwiftUI
- Xcode
- Photos framework
- Vision framework
- 外部サーバーなし
- 外部AIなし
- RevenueCat・課金なし
- 印刷サービス連携なし
- iOSシミュレータで動作確認できる構成
- 実装・テスト・修正のたびにGitの差分を確認する

## MVPの優先順位

以下の順番で実装してください。

1. SwiftUIアプリの起動
2. 旅行の開始日・終了日選択
3. サンプル写真による自動アルバム生成
4. 表紙とDayページの生成
5. 電子書籍のようなページめくりUI
6. 写真量に応じたページ数の自動調整
7. iPhone内の写真ライブラリ連携
8. Vision frameworkによる写真品質・類似性の評価
9. 印刷用PDFの生成またはPDF出力導線

最初からすべてを完璧に実装せず、まずサンプル写真でコア体験を完成させてください。

## 必須のユーザーフロー

ホーム画面
↓
旅行の開始日・終了日を選択
↓
生成中画面
↓
Travel Journalの表紙
↓
左右スワイプまたはタップでページをめくる
↓
最終ページ
↓
印刷用PDFへの導線

## デザイン方針

Travel Journalは、Pinterestのようなスクラップブック風にしてください。

- 紙のような背景
- 大きい写真と小さい写真の組み合わせ
- コラージュ
- 余白
- テープや紙の質感
- 表紙から始まる本らしい構成
- ページめくりアニメーション

単なる写真一覧やグリッド表示にはしないでください。

## 写真選定ルール

- 選択した旅行期間内の写真を対象にする
- 写真をユーザーに1枚ずつ選ばせない
- 動画は除外する
- スクリーンショットなど明らかに不要な写真は除外する
- Visionで低品質な写真を候補から外す
- 類似写真はまとめ、代表写真を選ぶ
- 写真が少ない日は余白を活かす
- 写真が多い日はページを増やす
- 位置情報があれば地名をタイトルに使う
- 位置情報がなければ `Day 1` などで表示する

## Fallback

以下を必ず用意してください。

- アプリ内に同梱したサンプル写真
- 固定のサンプル旅行データ
- 写真が0枚でもクラッシュしない処理
- Photos権限が拒否された場合の説明
- Vision処理に失敗した場合のルールベースFallback

## 作らない機能

- SNS共有
- アカウント
- クラウド同期
- AI旅行プラン
- 動画生成
- 課金
- RevenueCat
- 印刷サービスAPI連携
- 高度なAIタイトル生成

## 作業手順

1. リポジトリと既存ファイルを確認する
2. Xcodeプロジェクトの有無を確認する
3. 不足している場合はSwiftUIプロジェクトを作成する
4. まずサンプル写真でコアフローを実装する
5. iOSシミュレータで起動する
6. 明らかな起動・画面遷移・ビルドエラーを修正する
7. PhotosとVisionの連携を追加する
8. PDF出力を追加する
9. `xcodebuild`またはXcodeでビルド確認する
10. `git diff`を確認する
11. 変更内容と確認結果を報告する

## 完了条件

- アプリが起動する
- 開始日・終了日を選択できる
- 生成ボタンが動く
- アルバム生成画面が表示される
- 表紙が表示される
- Dayページが表示される
- 左右スワイプでページをめくれる
- タップでもページを進められる
- 写真が見切れずに表示される
- 写真量に応じてページ数が変わる
- 空データでクラッシュしない
- Photos権限拒否でクラッシュしない
- Vision失敗時にFallbackが動く
- PDF出力またはPDF出力への導線がある
- 明らかなSwift / Xcodeビルドエラーがない

まず実装を開始してください。大きな仕様変更が必要な場合だけ確認し、それ以外はDigLogの計画書に沿って判断してください。

---

## Codexへの追加指示

MVP完成後は、Claude Code DesktopにiOSシミュレータで実操作レビューをさせます。

Claudeのレビュー結果をもとに、Codexが難しいバグの修正と最終品質保証を担当します。
